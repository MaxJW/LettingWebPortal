@model IEnumerable<LWCustomer.Models.Enquired>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PropertyId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HasEnquired)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PropertyId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HasEnquired)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EnquiredId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EnquiredId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EnquiredId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
